function make_shirt(size: string, text: string) {
  console.log(
    `The size of shirt is ${size} and the text is printed on it is ${text}`
  );
}

make_shirt("S", "The Coder!");
make_shirt("M", "The Programer!");
make_shirt("L", "The Developer!");
